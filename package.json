{
  "name": "inmode",
  "version": "1.0.0",
  "private": true,
  "description": "inmode",
  "author": "kratoar",
  "keywords": [
    "gatsby"
  ],
  "scripts": {
    "develop": "gatsby develop",
    "start": "gatsby develop",
    "build": "gatsby build",
    "serve": "gatsby serve",
    "clean": "gatsby clean",
    "format": "prettier --write \"**/*.{js,jsx,ts,tsx,json,md}\"",
    "test": "echo \"Write tests! -> https://gatsby.dev/unit-testing\" && exit 1",
    "update_all": "npm update --save/--save-dev -f",
    "launch_dev": "cls && gatsby clean && NODE_ENV=development npm run develop",
    "launch_stage": "cls && gatsby clean && NODE_ENV=staging npm run develop",
    "launch_prod": "cls && gatsby clean && NODE_ENV=production npm run build && gatsby serve"
  },
  "dependencies": {
    "@pasdo501/gatsby-source-woocommerce": "^0.6.2",
    "gatsby": "file:latest/gatsby-cli@latest",
    "gatsby-plugin-anchor-links": "^1.2.1",
    "gatsby-plugin-manifest": "^2.12.1",
    "gatsby-plugin-react-helmet": "^3.10.0",
    "gatsby-plugin-sharp": "^2.14.4",
    "gatsby-plugin-sitemap": "^2.12.0",
    "gatsby-sharp": "^1.3.0",
    "gatsby-source-filesystem": "^2.11.1",
    "gatsby-source-strapi": "^3.0.4",
    "gatsby-transformer-sharp": "^2.12.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
