import React from "react";
import { BlogArticleContext_Interface } from "../interfaces";

export const ArticleContext:React.Context<BlogArticleContext_Interface> = React.createContext({
    
});

export default ArticleContext;