import React from "react";
import { UserContext_Interface } from "../interfaces";

export const UserContext:React.Context<UserContext_Interface> = React.createContext({
    
});

export default UserContext;